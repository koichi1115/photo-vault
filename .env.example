# Backend Environment Variables
PORT=3001
FRONTEND_URL=http://localhost:5173

# Anthropic API
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# AWS Configuration for Glacier Deep Archive
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
S3_BUCKET_NAME=glacier-photo-vault

# Note: Create an S3 bucket with the following configuration:
# 1. Create bucket in your AWS console
# 2. Enable versioning (optional)
# 3. Set up lifecycle policy to transition objects to Glacier Deep Archive immediately
# 4. Configure appropriate IAM permissions for s3:PutObject, s3:GetObject, s3:RestoreObject
